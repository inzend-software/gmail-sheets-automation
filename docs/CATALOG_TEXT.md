# Texto listo para Project Catalog / Upwork (Gmail → Sheets automation)

I build a reliable Gmail → Google Sheets automation that runs 24/7,
extracts fields from incoming emails, and keeps your spreadsheet perfectly updated — without manual work.

What you get (deliverable):
- Mapping of email fields to Sheet columns (date, sender, subject, body, custom fields)
- Secure deploy (FastAPI + Service Account) or Apps Script option
- Logging and simple error handling
- 24–48h delivery, 1–2 minor revisions included
- Short setup video and README for the client

Boundaries & Add-ons:
- Extra integrations (CRM, Slack routing) = add-on
- Custom extraction rules > 5 fields = add-on
- Full data migration or multiple mailboxes = add-on
